"use strict"

const chain = require("../src/chain")
const page = require("../src/page")

jest.mock("../src/page.js")

describe("Manage Chain", () => {
  test("Add new page", () => {

    chain.addPage(newPage)

    
  })
})
